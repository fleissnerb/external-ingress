defaultNamespace: authentik-remote-cluster
labels:
  bundle: authentik-remote-cluster
dependsOn:
  - selector:
      matchLabels:
        bundle: ClusterIssuer
helm:
  repo: https://charts.goauthentik.io
  chart: authentik-remote-cluster
  releaseName: authentik-remote-cluster
  version: "2.1.0"
  valuesFiles:
    - values.yaml